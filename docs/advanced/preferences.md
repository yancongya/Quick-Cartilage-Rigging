---
title: 高级配置：插件偏好设置
---

# 高级配置：插件偏好设置

"快速软骨绑定"插件提供了一些自定义选项，让您可以根据自己的工作流程和偏好来调整插件的界面和默认参数。这些设置都集中在插件的偏好设置面板中。

---

## 如何访问偏好设置

1.  在Blender顶部菜单栏中，选择 `编辑 (Edit)` > `偏好设置 (Preferences...)`。
2.  在弹出的窗口中，切换到 `插件 (Add-ons)` 选项卡。
3.  在右上角的搜索框中输入 `Quick Cartilage Rigging` 来找到本插件。
4.  点击插件条目左侧的小箭头，展开详细信息和偏好设置。

<!-- 建议您在这里放置一张偏好设置面板的截图 -->

---

## 可用设置详解

### 界面显示

*   **N面板 (N-Panel)**
    *   **作用**: 控制是否在3D视图右侧的侧边栏（按 `N` 键调出）中显示插件主面板。
    *   **默认值**: 开启。

*   **工具面板 (Tool-Panel)**
    *   **作用**: 控制是否在3D视图左侧的工具栏（按 `T` 键调出）中也显示一个插件面板的副本。
    *   **默认值**: 关闭。

*   **启用右键菜单 (Enable Right-Click Menu)**
    *   **作用**: 控制是否将插件的核心功能集成到骨架在对象、编辑和姿态模式下的右键上下文菜单中。对于喜欢使用快捷菜单的用户来说非常方便。
    *   **默认值**: 开启。

### 默认参数

*   **默认圆环缩放 (Default Circle Scale)**
    *   **作用**: 设置当您使用"生成FK绑定"功能时，所创建的控制器（圆环形状）的初始大小。
    *   **默认值**: `1.0`。

*   **默认追踪强度 (Default Damped Track Influence)**
    *   **作用**: 设置当您使用"生成软骨绑定"功能时，"难崩系数"的初始默认值。这个值决定了新创建的骨骼链的初始"软硬"程度。
    *   **默认值**: `0.6`。